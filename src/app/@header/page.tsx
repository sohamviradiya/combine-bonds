"use client";

import { Typography } from "@mui/material";

export default function Header() {
    return (
        <Typography variant="h1" gutterBottom>
            Header
        </Typography>
    );
}
